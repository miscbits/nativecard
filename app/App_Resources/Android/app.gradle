// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.misbcits.flashcards"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
